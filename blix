# #!/usr/bin/env bash

if [ $# -eq 0 ]; then
    echo "Usage: blix <file.blix>"
    exit 1
fi
BLIX_FILE="$1"
if [ ! -f "$BLIX_FILE" ]; then
    echo "Error: File '$BLIX_FILE' not found"
    exit 1
fi
cp "$BLIX_FILE" examples/hello_world.blix
flix run
